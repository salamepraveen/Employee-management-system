server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ems_payments?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      connection-timeout: 20000

app:
  order-service-url: http://localhost:8083